#include <iostream>
#include <vector>

#include <BRDFReaderTest.h>
#include <OptimisationTest.h>
#include <ParametrisationTest.h>

/*bool find(std::string stringToFind, std::vector<std::string> vecToSearchIn) {
    for (uint i = 0; i < vecToSearchIn.size(); i++) {
        if (stringToFind.compare(vecToSearchIn[i]) == 0) return true;
    }
    return false;
}*/

int main(int argv, char* argc[]){

    /*std::vector<std::string> opt = {};
    for (int i = 1; i < argv; i ++) {
        opt.emplace_back(std::string(argc[i]));
    }

    if (opt.size() == 0) {
        std::cout << "There is currently no tests to execute." << std::endl;
        std::cout << "If you want to lunch a test please do : " << std::endl;
        std::cout << argc[0] << " [Test1_name [Test2_name [...]]]" << std::endl;
    }*/

    /* Test exemple
    if (find("Test_name", opt)) {
        Tests you want to do
    }
    */
    return 0;
}
