#include "OptiDataWriter.h"
#include "Albedo.h"

/**
 * @file OptiDataWriter.hpp
 */

namespace ChefDevr
{
    void writeAlbedoMap (const std::string& path, const unsigned width = 1024, const unsigned height = 1024)
    {
        /*Color color;
        Vector<Scalar> brdf;
        for (;;) {
            color = computeAlbedo(brdf);
        }*/
    }
} // namespace ChevDevr  
